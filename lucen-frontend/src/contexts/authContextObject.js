// src/contexts/authContextObject.js
import { createContext } from "react";

// Create and export the context object
// This file now only exports a non-component value.
export const AuthContext = createContext(null);
